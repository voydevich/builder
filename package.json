{
  "name": "@voydevichb/builder",
  "version": "1.0.4",
  "main": "index.js",
  "scripts": {
    "build": "echo 'build'",
    "patch-release": "npm version patch && npm publish",
    "git-commit": "git add -A . && git commit -a -m 'gh-pages update'",
    "git-push": "git push origin master --force && git checkout master",
    "deploy": "npm run build && npm run git-commit && npm run git-push && npm run patch-release"
  },
  "bin": {
    "website": "website/website.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "commander": "^2.19.0",
    "path": "^0.12.7"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/voydevich/builder.git"
  }
}
